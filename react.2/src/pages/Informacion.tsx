import { Typography, Box } from "@mui/material";

export default () => (

  <Box sx={{ p: 3 }}>
    <Typography variant="h4">INFORMACIÓN</Typography>
    <Typography>Práctica 10: Router, FormData, useContext.</Typography>
  </Box>
);
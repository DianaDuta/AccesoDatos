import { Typography, Box } from "@mui/material";

export default () => (

  <Box sx={{ p: 3 }}>
    <Typography variant="h4"> ¡BIENVENIDO!</Typography>
    <Typography>Esto es una app demo con React Router & Context.</Typography>
  </Box>
);